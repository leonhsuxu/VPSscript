name: TG频道和群抓取节点测速 XC-speedtest测速版

# ==================== 触发条件配置 ====================
on:
  # 定时执行配置
  schedule:
    # 每4小时执行一次，整点后的10分钟执行（例如：00:10, 04:10, 08:10...）
    # 使用UTC时间，北京时间需要+8小时
    - cron: '10 */4 * * *'
  
  # 支持手动触发工作流
  workflow_dispatch:
    inputs:
      # 网络配置模式选择
      warp_config:
        description: '选择网络配置模式'
        required: false
        default: 'default'
        type: choice
        options:
          - default        # 默认配置：抓取用GitHub，测速用Warp
          - all_warp       # 全流程使用Warp网络
          - all_github     # 全流程使用GitHub网络
          - test_mode      # 测试模式（只抓取少量节点）
      
      # 是否强制重新下载工具
      force_download:
        description: '强制重新下载工具（忽略缓存）'
        required: false
        default: false
        type: boolean
      
      # 测速模式选择
      speedtest_mode:
        description: '选择测速模式'
        required: false
        default: 'tcp_first'
        type: choice
        options:
          - tcp_first     # 先TCP粗筛，再Speedtest精测（推荐）
          - tcp_only      # 只用TCP测速（最快）
          - clash_only    # 只用Speedtest测速（最准）
          - clash_first   # 先Speedtest，再TCP验证

# ==================== 环境变量配置 ====================
env:
  # ==================== 版本配置 ====================
  MIHOMO_VERSION: v1.19.17           # Mihomo (Clash Meta) 版本
  SPEEDTEST_VERSION: v0.4.1          # XC Speedtest 版本
  WGCF_VERSION: v2.2.29              # WGCF (Cloudflare Warp) 版本
  
  # ==================== 网络控制配置 ====================
  # 这些变量是Python脚本的唯一控制源，控制各阶段是否使用Warp网络
  # true=使用Warp网络，false=使用原始GitHub网络
  
  # 抓取阶段：使用GitHub网络（速度快）
  WARP_FOR_SCRAPING: "false"
  
  # TCP测速阶段：使用Warp网络（模拟国内环境，测速结果更准确）
  WARP_FOR_TCP: "true"
  
  # Speedtest测速阶段：使用Warp网络（模拟国内环境，测速结果更准确）
  WARP_FOR_SPEEDTEST: "true"
  
  # 最终处理阶段：使用GitHub网络（确保提交等操作正常）
  WARP_FOR_FINAL: "false"
  
  # ==================== 测速策略配置 ====================
  # 测速模式选项：
  #   "tcp_only"      → 只用 TCP 测速（最快，最严格，适合节点特别多的情况）
  #   "clash_only"    → 只用 Speedtest-clash 测速（最准）
  #   "tcp_first"     → 先 TCP 粗筛（<800ms）→ 再 Speedtest 精测（推荐！平衡速度与质量）
  #   "clash_first"   → 先 Speedtest → 再 TCP（一般用不上）
  SPEEDTEST_MODE: "tcp_first"
  
  # ==================== 带宽筛选配置 ====================
  # 是否启用带宽筛选（True=启用，False=关闭）
  ENABLE_BANDWIDTH_FILTER: "true"
  
  # 最低带宽阈值（单位：MB/s）
  # 只保留带宽大于等于此值的节点
  MIN_BANDWIDTH_MB: "25"
  
  # ==================== 日志配置 ====================
  # 是否启用TCP测速详细日志（生产环境建议关闭）
  ENABLE_TCP_LOG: "false"
  
  # 是否启用Speedtest测速详细日志（生产环境建议关闭）
  ENABLE_SPEEDTEST_LOG: "false"
  
  # ==================== Telegram频道配置 ====================
  # 要抓取的Telegram频道列表（每行一个）
  # 支持频道ID（如 @channel_name）或频道链接
  # 以 | 符号分隔，支持多行
  TELEGRAM_CHANNEL_IDS: |
    @fqDINYUE
    @wxdy666
    @freeyule
    @ccbaohe
    @duangvpsfs         
    @zzzjjjkkkoi
    @xmdexw
    @v2v2clash
    @dingyue_Center
    @tempssrlink
    @sorenab2
    @proxyshareCN

# ==================== 工作流任务定义 ====================
jobs:
  # 主构建任务
  build:
    # 使用最新的Ubuntu运行器
    runs-on: ubuntu-latest
    
    # 设置任务超时时间（单位：分钟）
    timeout-minutes: 30
    
    # 任务步骤定义
    steps:
      # ==================== 步骤1：检出代码仓库 ====================
      - name: 检出代码仓库
        uses: actions/checkout@v4
        with:
          # 获取完整的历史记录，便于后续的提交操作
          fetch-depth: 0
          # 使用GitHub Token进行认证
          token: ${{ secrets.GITHUB_TOKEN }}
          # 是否检出子模块
          submodules: false
          # 是否使用稀疏检出
          sparse-checkout: |
            TelegramNode/
            flclashyaml/
            requirements.txt
          
        # 显示步骤执行时间
        timeout-minutes: 2
      
      # ==================== 步骤2：设置Python环境 ====================
      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          # 指定Python版本（3.11）
          python-version: '3.11'
          # 启用缓存以加速依赖安装
          cache: 'pip'
          # 缓存依赖文件路径
          cache-dependency-path: '**/requirements.txt'
          # 架构类型
          architecture: 'x64'
        
        # 显示步骤执行时间
        timeout-minutes: 2
      
      # ==================== 步骤3：缓存Python依赖 ====================
      - name: 缓存 Python 依赖
        uses: actions/cache@v4
        id: cache-pip
        with:
          # 缓存pip安装包的位置
          path: |
            ~/.cache/pip
            ~/.local/lib/python3.11/site-packages
          # 缓存的唯一标识键，基于requirements.txt的哈希值
          key: ${{ runner.os }}-python-${{ hashFiles('requirements.txt') }}
          # 如果没有完全匹配的缓存，尝试恢复最接近的缓存
          restore-keys: |
            ${{ runner.os }}-python-
            ${{ runner.os }}-pip-
        
        # 显示步骤执行时间
        timeout-minutes: 1
      
      # ==================== 步骤4：安装Python依赖及系统依赖 ====================
      - name: 安装 Python 依赖及系统依赖
        run: |
          # 显示步骤开始
          echo "=== 🛠️ 安装依赖开始 ==="
          
          # 升级pip到最新版本
          echo "1. 升级pip..."
          python -m pip install --upgrade pip
          
          # 安装requirements.txt中指定的Python包
          echo "2. 安装Python依赖..."
          pip install -r requirements.txt
          
          # 更新系统包列表（静默模式）
          echo "3. 更新系统包列表..."
          sudo apt-get update -qq
          
          # 安装必要的系统工具
          echo "4. 安装系统工具..."
          # - wireguard-tools: Warp VPN所需
          # - curl: 网络请求工具
          # - resolvconf: DNS解析配置
          # - wget: 下载工具
          # - net-tools: 网络工具
          sudo apt-get install -y \
            wireguard-tools \
            curl \
            resolvconf \
            wget \
            net-tools \
            dnsutils
          
          # 显示已安装的Python包列表（用于调试）
          echo "5. 已安装的Python包:"
          pip list | head -20
          
          echo "=== ✅ 依赖安装完成 ==="
        
        # 设置步骤超时
        timeout-minutes: 5
      
      # ==================== 步骤5：缓存Mihomo程序 ====================
      - name: 缓存 Mihomo 程序
        id: cache-mihomo
        uses: actions/cache@v4
        with:
          # 缓存Mihomo二进制文件
          path: mihomo
          # 缓存键：基于架构和版本
          key: mihomo-${{ runner.os }}-amd64-${{ env.MIHOMO_VERSION }}
          # 恢复键：相同架构的不同版本
          restore-keys: |
            mihomo-${{ runner.os }}-amd64-${{ env.MIHOMO_VERSION }}
            mihomo-${{ runner.os }}-amd64-
        
        # 显示步骤执行时间
        timeout-minutes: 1
      
      # ==================== 步骤6：下载Mihomo指定版本 ====================
      - name: 下载 Mihomo 指定版本
        # 只有缓存未命中或强制下载时才执行此步骤
        if: steps.cache-mihomo.outputs.cache-hit != 'true' || inputs.force_download == true
        run: |
          # 启用错误检测，任何命令失败即停止
          set -e
          
          # 获取Mihomo版本标签
          TAG="${{ env.MIHOMO_VERSION }}"
          
          # 构建下载URL
          URL="https://github.com/MetaCubeX/mihomo/releases/download/${TAG}/mihomo-linux-amd64-${TAG}.gz"
          
          echo "=== 📥 下载 Mihomo 开始 ==="
          echo "版本: $TAG"
          echo "URL: $URL"
          
          # 检查URL是否可访问
          echo "1. 检查下载链接..."
          if ! curl --head --fail --silent "$URL" > /dev/null 2>&1; then
            echo "❌ 错误：下载地址不可用！"
            echo "请检查版本号 $TAG 是否正确"
            exit 1
          fi
          
          # 下载Mihomo压缩包（重试3次，超时30秒）
          echo "2. 下载Mihomo..."
          wget --tries=3 --timeout=30 --show-progress -O mihomo.gz "$URL"
          
          # 解压文件
          echo "3. 解压文件..."
          gunzip -f mihomo.gz
          
          # 添加执行权限
          echo "4. 添加执行权限..."
          chmod +x mihomo
          
          # 验证版本
          echo "5. 验证版本..."
          ./mihomo -v || echo "版本信息可能无法显示"
          
          # 验证文件类型
          echo "6. 验证文件..."
          file mihomo
          
          echo "=== ✅ Mihomo 下载完成 ==="
        
        # 设置步骤超时
        timeout-minutes: 3
      
      # ==================== 步骤7：缓存wgcf工具 ====================
      - name: 缓存 wgcf 工具
        id: cache-wgcf
        uses: actions/cache@v4
        with:
          # 缓存wgcf二进制文件
          path: wgcf
          # 缓存键：基于架构和版本
          key: wgcf-${{ runner.os }}-amd64-${{ env.WGCF_VERSION }}
          # 恢复键
          restore-keys: |
            wgcf-${{ runner.os }}-amd64-${{ env.WGCF_VERSION }}
            wgcf-${{ runner.os }}-amd64-
        
        # 显示步骤执行时间
        timeout-minutes: 1
      
      # ==================== 步骤8：下载wgcf工具 ====================
      - name: 下载 wgcf 工具
        if: steps.cache-wgcf.outputs.cache-hit != 'true' || inputs.force_download == true
        run: |
          # 获取完整版本号（带v前缀）
          WGCF_FULL_VERSION="${{ env.WGCF_VERSION }}"
          
          # 去除v前缀，用于文件名
          WGCF_FILENAME_VERSION="${WGCF_FULL_VERSION#v}"
          
          echo "=== 📥 下载 wgcf 开始 ==="
          echo "版本: v${WGCF_FILENAME_VERSION}"
          
          # 构建下载URL
          URL="https://github.com/ViRb3/wgcf/releases/download/${WGCF_FULL_VERSION}/wgcf_${WGCF_FILENAME_VERSION}_linux_amd64"
          
          # 下载wgcf
          echo "1. 下载wgcf..."
          curl -fsSL --progress-bar -o wgcf "$URL"
          
          # 检查文件是否下载成功
          echo "2. 验证文件..."
          if [ ! -f "wgcf" ]; then
            echo "❌ 错误：wgcf 下载失败！"
            echo "请检查版本号 $WGCF_FULL_VERSION 是否正确"
            exit 1
          fi
          
          # 添加执行权限
          echo "3. 添加执行权限..."
          chmod +x wgcf
          
          # 验证文件
          echo "4. 文件信息："
          file wgcf || echo "文件信息获取失败"
          
          # 测试版本
          echo "5. 测试版本信息..."
          ./wgcf --version || echo "版本信息可能无法显示"
          
          echo "=== ✅ wgcf 下载完成 ==="
        
        # 设置步骤超时
        timeout-minutes: 3
      
      # ==================== 步骤9：缓存speedtest-clash程序 ====================
      - name: 缓存 xiecang speedtest-clash
        id: cache-xc
        uses: actions/cache@v4
        with:
          # 缓存speedtest-clash二进制文件
          path: xcspeedtest
          # 缓存键：基于版本
          key: xiecang-speedtest-${{ env.SPEEDTEST_VERSION }}
          # 恢复键
          restore-keys: |
            xiecang-speedtest-${{ env.SPEEDTEST_VERSION }}
            xiecang-speedtest-
        
        # 显示步骤执行时间
        timeout-minutes: 1
      
      # ==================== 步骤10：下载speedtest-clash指定版本 ====================
      - name: 下载 xiecang speedtest-clash
        if: steps.cache-xc.outputs.cache-hit != 'true' || inputs.force_download == true
        run: |
          # 启用错误检测
          set -e
          
          # 获取发布版本标签
          RELEASE_TAG="${{ env.SPEEDTEST_VERSION }}"
          
          echo "=== 📥 下载 speedtest-clash 开始 ==="
          echo "版本: $RELEASE_TAG"
          
          # 获取指定版本的下载URL
          echo "1. 获取下载URL..."
          URL=$(curl -s "https://api.github.com/repos/xiecang/speedtest-clash/releases/tags/${RELEASE_TAG}" \
                | grep "browser_download_url.*speedtest-clash_Linux_x86_64" | cut -d '"' -f4)
          
          # 如果找不到指定版本，尝试使用最新版本
          if [ -z "$URL" ]; then
            echo "⚠️ 警告: 未找到指定版本 (${RELEASE_TAG})，尝试使用最新版本。"
            URL=$(curl -s https://api.github.com/repos/xiecang/speedtest-clash/releases/latest \
                | grep "browser_download_url.*speedtest-clash_Linux_x86_64" | cut -d '"' -f4)
          fi
          
          # 如果仍然找不到URL，报错退出
          if [ -z "$URL" ]; then
            echo "❌ 错误：未能找到 speedtest-clash 下载 URL，退出。"
            exit 1
          fi
          
          echo "2. 下载URL: $URL"
          
          # 下载文件（重试3次，超时60秒）
          echo "3. 下载文件..."
          wget --tries=3 --timeout=60 --show-progress -O downloaded_file "$URL"
          
          # 检查文件类型并处理
          echo "4. 处理文件..."
          if file downloaded_file | grep -q "gzip compressed data"; then
            echo "   检测到压缩包，开始解压"
            tar -xzf downloaded_file
            
            # 查找可执行文件
            if [ -f "speedtest-clash" ]; then
              mv speedtest-clash xcspeedtest
            else
              # 尝试找到其他可执行文件
              EXE_PATH=$(find . -maxdepth 1 -type f -executable -name "speedtest-clash*" -print -quit)
              if [ -n "$EXE_PATH" ]; then
                mv "$EXE_PATH" xcspeedtest
              else
                echo "❌ 错误：未找到 speedtest-clash 可执行文件"
                exit 1
              fi
            fi
            # 清理临时文件
            rm downloaded_file
          elif file downloaded_file | grep -q "executable"; then
            echo "   下载的是可执行文件，直接重命名为 xcspeedtest"
            mv downloaded_file xcspeedtest
          else
            echo "❌ 错误：下载的文件格式未知"
            file downloaded_file
            exit 1
          fi
          
          # 添加执行权限
          echo "5. 添加执行权限..."
          chmod +x xcspeedtest
          
          # 验证程序可用性
          echo "6. 验证程序..."
          ./xcspeedtest --version || ./xcspeedtest --help || echo "帮助信息可能无法显示"
          
          echo "=== ✅ speedtest-clash 下载完成 ==="
        
        # 设置步骤超时
        timeout-minutes: 5
      
      # ==================== 步骤11：智能网络配置（根据手动触发调整） ====================
      - name: 智能网络配置
        run: |
          echo "=== 🌐 智能网络配置 ==="
          echo "触发方式: ${{ github.event_name }}"
          
          # 根据手动触发输入调整网络配置
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "手动触发工作流，网络配置模式: ${{ inputs.warp_config }}"
            
            case "${{ inputs.warp_config }}" in
              "default")
                echo "使用默认网络配置"
                # 保持env中的默认设置
                echo "配置: 抓取用GitHub，测速用Warp，最终用GitHub"
                ;;
                
              "all_warp")
                echo "全流程使用Warp网络"
                # 设置所有阶段都使用Warp
                echo "WARP_FOR_SCRAPING=true" >> $GITHUB_ENV
                echo "WARP_FOR_TCP=true" >> $GITHUB_ENV
                echo "WARP_FOR_SPEEDTEST=true" >> $GITHUB_ENV
                echo "WARP_FOR_FINAL=true" >> $GITHUB_ENV
                echo "配置: 全流程使用Warp网络"
                ;;
                
              "all_github")
                echo "全流程使用GitHub网络"
                # 设置所有阶段都不使用Warp
                echo "WARP_FOR_SCRAPING=false" >> $GITHUB_ENV
                echo "WARP_FOR_TCP=false" >> $GITHUB_ENV
                echo "WARP_FOR_SPEEDTEST=false" >> $GITHUB_ENV
                echo "WARP_FOR_FINAL=false" >> $GITHUB_ENV
                echo "配置: 全流程使用GitHub网络"
                ;;
                
              "test_mode")
                echo "测试模式：减少抓取数量，使用Warp测速"
                # 修改频道列表，只抓取前3个频道
                echo "TELEGRAM_CHANNEL_IDS=\"@fqDINYUE\n@wxdy666\n@freeyule\"" >> $GITHUB_ENV
                echo "WARP_FOR_SCRAPING=false" >> $GITHUB_ENV
                echo "WARP_FOR_TCP=true" >> $GITHUB_ENV
                echo "WARP_FOR_SPEEDTEST=true" >> $GITHUB_ENV
                echo "WARP_FOR_FINAL=false" >> $GITHUB_ENV
                echo "配置: 测试模式，减少抓取数量"
                ;;
            esac
            
            # 如果手动指定了测速模式，则覆盖默认值
            if [ -n "${{ inputs.speedtest_mode }}" ]; then
              echo "SPEEDTEST_MODE=${{ inputs.speedtest_mode }}" >> $GITHUB_ENV
              echo "测速模式覆盖为: ${{ inputs.speedtest_mode }}"
            fi
          else
            echo "定时触发，使用默认网络配置"
          fi
          
          # 显示最终的网络配置
          echo ""
          echo "📋 最终执行配置:"
          echo "--------------------------------------------------"
          echo "网络配置:"
          echo "  - 抓取阶段 Warp: ${{ env.WARP_FOR_SCRAPING }}"
          echo "  - TCP测速 Warp: ${{ env.WARP_FOR_TCP }}"
          echo "  - Speedtest测速 Warp: ${{ env.WARP_FOR_SPEEDTEST }}"
          echo "  - 最终阶段 Warp: ${{ env.WARP_FOR_FINAL }}"
          echo ""
          echo "测速配置:"
          echo "  - 测速模式: ${{ env.SPEEDTEST_MODE }}"
          echo "  - 带宽筛选: ${{ env.ENABLE_BANDWIDTH_FILTER }} (≥${{ env.MIN_BANDWIDTH_MB }}MB/s)"
          echo "  - TCP日志: ${{ env.ENABLE_TCP_LOG }}"
          echo "  - Speedtest日志: ${{ env.ENABLE_SPEEDTEST_LOG }}"
          echo "--------------------------------------------------"
          echo "=== ✅ 网络配置完成 ==="
        
        # 设置步骤超时
        timeout-minutes: 2
      
      # ==================== 步骤12：运行Telegram抓取及测速脚本 ====================
      - name: 运行 Telegram 抓取及 Clash 测速脚本
        env:
          # ==================== Telegram API 凭证 ====================
          # 从GitHub Secrets读取，确保安全性
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          TELEGRAM_STRING_SESSION: ${{ secrets.TELEGRAM_STRING_SESSION }}
          
          # ==================== 网络控制变量 ====================
          # 这些变量会被Python脚本读取，控制各阶段网络行为
          WARP_FOR_SCRAPING: ${{ env.WARP_FOR_SCRAPING }}
          WARP_FOR_TCP: ${{ env.WARP_FOR_TCP }}
          WARP_FOR_SPEEDTEST: ${{ env.WARP_FOR_SPEEDTEST }}
          WARP_FOR_FINAL: ${{ env.WARP_FOR_FINAL }}
          
          # ==================== 测速配置 ====================
          SPEEDTEST_MODE: ${{ env.SPEEDTEST_MODE }}
          ENABLE_TCP_LOG: ${{ env.ENABLE_TCP_LOG }}
          ENABLE_SPEEDTEST_LOG: ${{ env.ENABLE_SPEEDTEST_LOG }}
          
          # ==================== 带宽筛选配置 ====================
          ENABLE_BANDWIDTH_FILTER: ${{ env.ENABLE_BANDWIDTH_FILTER }}
          MIN_BANDWIDTH_MB: ${{ env.MIN_BANDWIDTH_MB }}
          
          # ==================== 其他环境变量 ====================
          # Telegram频道列表
          TELEGRAM_CHANNEL_IDS: ${{ env.TELEGRAM_CHANNEL_IDS }}
          
          # 标识当前在GitHub Actions环境中运行
          # Python脚本会检查这个变量来决定是否启用网络切换功能
          GITHUB_ACTIONS: "true"
          
          # Python输出缓冲设置（确保实时输出日志）
          PYTHONUNBUFFERED: "1"
          
        run: |
          # 创建输出目录
          mkdir -p flclashyaml
          
          echo "=== 🚀 开始执行节点抓取和测速 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S %Z')"
          echo ""
          
          # 显示当前目录结构（用于调试）
          echo "📁 当前目录结构:"
          ls -la
          echo ""
          
          # 检查必要文件是否存在
          echo "🔍 检查必要文件:"
          if [ -f "TelegramNode/TBD-telegram_publiclink.py" ]; then
            echo "  ✅ Python脚本存在"
          else
            echo "  ❌ Python脚本不存在"
            exit 1
          fi
          
          if [ -f "requirements.txt" ]; then
            echo "  ✅ 依赖文件存在"
          else
            echo "  ❌ 依赖文件不存在"
            exit 1
          fi
          
          # 检查工具是否存在
          echo "🛠️  检查工具:"
          if [ -f "xcspeedtest" ] && [ -x "xcspeedtest" ]; then
            echo "  ✅ speedtest-clash 存在且可执行"
          else
            echo "  ⚠️  speedtest-clash 不存在或不可执行"
          fi
          
          if [ -f "wgcf" ] && [ -x "wgcf" ]; then
            echo "  ✅ wgcf 存在且可执行"
          else
            echo "  ⚠️  wgcf 不存在或不可执行"
          fi
          
          echo ""
          echo "📊 执行参数汇总:"
          echo "--------------------------------------------------"
          echo "网络配置:"
          echo "  📡 抓取阶段: $( [ "$WARP_FOR_SCRAPING" = "true" ] && echo "使用Warp" || echo "使用GitHub" )"
          echo "  🚀 TCP测速: $( [ "$WARP_FOR_TCP" = "true" ] && echo "使用Warp" || echo "使用GitHub" )"
          echo "  ⚡ Speedtest: $( [ "$WARP_FOR_SPEEDTEST" = "true" ] && echo "使用Warp" || echo "使用GitHub" )"
          echo "  📝 最终处理: $( [ "$WARP_FOR_FINAL" = "true" ] && echo "使用Warp" || echo "使用GitHub" )"
          echo ""
          echo "测速配置:"
          echo "  🎯 测速模式: $SPEEDTEST_MODE"
          echo "  📶 带宽筛选: $( [ "$ENABLE_BANDWIDTH_FILTER" = "true" ] && echo "启用 (≥${MIN_BANDWIDTH_MB}MB/s)" || echo "关闭" )"
          echo "  📋 日志级别: TCP=$ENABLE_TCP_LOG, Speedtest=$ENABLE_SPEEDTEST_LOG"
          echo "--------------------------------------------------"
          echo ""
          
          # 执行Python脚本
          echo "▶️ 开始执行Python脚本..."
          echo "--------------------------------------------------"
          
          # 使用time命令统计执行时间
          time python TelegramNode/TBD-telegram_publiclink.py
          
          EXIT_CODE=$?
          echo ""
          echo "--------------------------------------------------"
          
          if [ $EXIT_CODE -eq 0 ]; then
            echo "✅ 节点抓取和测速完成"
            
            # 检查生成的配置文件
            if [ -f "flclashyaml/Tg-node1.yaml" ]; then
              FILE_SIZE=$(stat -c%s "flclashyaml/Tg-node1.yaml" 2>/dev/null || stat -f%z "flclashyaml/Tg-node1.yaml" 2>/dev/null || echo "unknown")
              echo "📁 配置文件生成成功: flclashyaml/Tg-node1.yaml (${FILE_SIZE} bytes)"
              
              # 显示文件前几行（预览）
              echo "📄 配置文件预览:"
              head -20 flclashyaml/Tg-node1.yaml
            else
              echo "⚠️  配置文件未生成"
            fi
          else
            echo "❌ Python脚本执行失败，退出码: $EXIT_CODE"
            exit $EXIT_CODE
          fi
          
          echo ""
          echo "结束时间: $(date '+%Y-%m-%d %H:%M:%S %Z')"
          echo "=== ✅ 脚本执行完成 ==="
        
        # 设置步骤超时（最长运行时间）
        timeout-minutes: 20
      
      # ==================== 步骤13：提交前清理Warp连接 ====================
      - name: 提交前清理 Warp 连接
        # 无论之前步骤成功或失败，都执行此步骤
        if: always()
        run: |
          echo "=== 🧹 提交前清理 WARP 连接 ==="
          echo "清理时间: $(date '+%Y-%m-%d %H:%M:%S %Z')"
          echo ""
          
          # 1. 检查当前网络状态
          echo "1️⃣ 检查当前网络状态:"
          CURRENT_IP=$(curl -4 -s --max-time 5 https://ip.sb 2>/dev/null || echo "无法获取")
          echo "   当前出口 IP: $CURRENT_IP"
          
          # 2. 停止Warp连接
          echo "2️⃣ 停止 WARP VPN..."
          sudo wg-quick down wgcf 2>/dev/null || echo "   WARP未运行或停止失败"
          
          # 3. 删除Warp接口
          echo "3️⃣ 清理网络接口..."
          sudo ip link delete wgcf 2>/dev/null || echo "   接口不存在或删除失败"
          
          # 4. 清理智能路由
          echo "4️⃣ 清理智能路由配置..."
          # GitHub IP范围列表
          github_ranges=(
            "140.82.112.0/20"
            "185.199.108.0/22"
            "185.199.109.0/22"
            "185.199.110.0/22"
            "185.199.111.0/22"
            "192.30.252.0/22"
            "192.30.253.0/22"
            "192.30.254.0/22"
            "192.30.255.0/22"
          )
          
          cleaned_count=0
          for cidr in "${github_ranges[@]}"; do
            sudo ip route del $cidr 2>/dev/null && cleaned_count=$((cleaned_count + 1)) || true
          done
          echo "   已清理 $cleaned_count 个GitHub路由"
          
          # 5. 刷新DNS缓存
          echo "5️⃣ 刷新DNS缓存..."
          sudo systemctl restart systemd-resolved 2>/dev/null || echo "   DNS刷新失败"
          
          # 6. 测试GitHub连接
          echo "6️⃣ 测试GitHub连接..."
          echo "   DNS解析测试:"
          timeout 5 nslookup github.com 2>&1 | head -3 || echo "   DNS解析失败"
          
          echo "   HTTP连接测试:"
          timeout 5 curl -I --silent https://github.com 2>&1 | head -2 || echo "   HTTP连接失败"
          
          # 7. 等待网络稳定
          echo "7️⃣ 等待网络稳定..."
          sleep 3
          
          # 8. 验证清理结果
          echo "8️⃣ 验证清理结果:"
          FINAL_IP=$(curl -4 -s --max-time 5 https://ip.sb 2>/dev/null || echo "无法获取")
          echo "   最终出口 IP: $FINAL_IP"
          
          if echo "$FINAL_IP" | grep -q "162.159\|172.64"; then
            echo "   ⚠️  检测到可能仍是Warp IP，但已尽力清理"
          else
            echo "   ✅ 清理完成，已恢复原始网络"
          fi
          
          echo ""
          echo "=== ✅ 网络清理完成 ==="
        
        # 设置步骤超时
        timeout-minutes: 3
      
      # ==================== 步骤14：提交生成的Clash配置到仓库 ====================
      - name: 提交生成的 Clash 配置到仓库
        # 只在抓取测速脚本成功执行后才提交
        if: success()
        run: |
          echo "=== 📤 提交变更到GitHub ==="
          echo "提交时间: $(date '+%Y-%m-%d %H:%M:%S %Z')"
          echo ""
          
          # 检查配置文件是否有变化
          echo "🔍 检查文件变更..."
          if git status --porcelain | grep -q "flclashyaml/Tg-node1.yaml"; then
            echo "✅ 检测到配置文件变更"
            
            # 配置Git用户信息（使用GitHub Actions的bot账户）
            echo "👤 配置Git用户..."
            git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git config --local user.name "github-actions[bot]"
            
            # 添加更改的文件
            echo "📝 添加变更文件..."
            git add flclashyaml/Tg-node1.yaml
            
            # 生成提交信息
            echo "📋 生成提交信息..."
            
            # 获取北京时间
            COMMIT_TIME=$(python3 -c "
from datetime import datetime, timedelta
print((datetime.utcnow() + timedelta(hours=8)).strftime('%Y-%m-%d %H:%M:%S CST'))
")
            
            # 统计节点数量和质量信息
            NODE_INFO=$(python3 -c "
import yaml, sys, json

try:
    with open('flclashyaml/Tg-node1.yaml', 'r', encoding='utf-8') as f:
        data = yaml.safe_load(f)
        
        # 节点数量
        proxies = data.get('proxies', [])
        node_count = len(proxies) if isinstance(proxies, list) else 0
        
        # 质量信息
        avg_quality = data.get('average_quality', 'N/A')
        quality_stats = data.get('quality_stats', {})
        
        # 网络配置
        warp_tcp = data.get('speedtest_config', {}).get('warp_for_tcp', 'N/A')
        warp_speedtest = data.get('speedtest_config', {}).get('warp_for_speedtest', 'N/A')
        
        # 构建质量字符串
        quality_str = ''
        if isinstance(quality_stats, dict):
            parts = []
            for key, value in quality_stats.items():
                parts.append(f'{key}{value}')
            if parts:
                quality_str = '[' + ' '.join(parts) + ']'
        
        print(f'{node_count} nodes | {avg_quality}分 {quality_str} | TCP_Warp={warp_tcp} Speedtest_Warp={warp_speedtest}')
        
except Exception as e:
    print(f'读取失败: {str(e)[:50]}')
    print('unknown nodes')
")
            
            # 生成提交信息
            COMMIT_MESSAGE="🤖 自动更新节点配置 ${COMMIT_TIME} (${NODE_INFO})"
            
            echo "提交信息: $COMMIT_MESSAGE"
            
            # 提交更改
            echo "💾 提交更改..."
            git commit
