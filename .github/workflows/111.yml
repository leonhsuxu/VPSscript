name: 🔄 丑团111
on:
  schedule:
    # 每周1/3/5 UTC 0:00 (北京时间 8:00) 自动运行
    - cron: '0 0 * * 1,3,5'
  workflow_dispatch: # 允许手动触发
  push:
    branches:
      - main
    paths:
      - 'flclashyaml/111.py'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置 Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        # 注意：增加了 PySocks 依赖，用于通过SOCKS5代理发送请求
        pip install requests pyyaml pycountry PySocks

    # --- 新步骤：下载并准备 Xray-core ---
    - name: 下载并准备 Xray-core
      run: |
        echo "正在下载最新的 Xray-core..."
        # 使用官方的一键安装脚本来获取最新版本
        bash -c "$(curl -L https://github.com/XTLS/Xray-install/raw/main/install-release.sh)" @ install -l --version 1.8.4
        
        # 脚本会将 xray 安装到 /usr/local/bin/xray
        # 我们需要把它移动到 Python 脚本期望的位置并重命名
        mv /usr/local/bin/xray flclashyaml/xray-core
        
        # 赋予执行权限（虽然安装脚本通常已设置，但再次确认无妨）
        chmod +x flclashyaml/xray-core
        
        echo "Xray-core 准备就绪。"

    - name: 运行合并与重命名脚本
      run: |
        python flclashyaml/111.py

    - name: 提交更新到仓库
      run: |
        git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add flclashyaml/choutuan-111.yaml
        if ! git diff --staged --quiet; then
          git commit -m "🔄 丑团yyy $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          git push
        else
          echo "文件无变化，无需提交。"
        fi
