name: 🔄 丑团111
on:
  schedule:
    # 每周1/3/5 UTC 0:00 (北京时间 8:00) 自动运行
    - cron: '0 0 * * 1,3,5'
  workflow_dispatch: # 允许手动触发
  push:
    branches:
      - main
    paths:
      - 'flclashyaml/111.py'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置 Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install requests pyyaml pycountry

    # --- 修正后的核心下载步骤 ---
    - name: 下载并准备 Clash.Meta Core
      run: |
        echo "正在确定最新的 Clash.Meta 版本..."
        # 使用 GitHub API 自动获取最新的 Clash.Meta 发行版下载链接
        # 这种方法更稳定，不会因为版本更新而失效
        LATEST_API_URL="https://api.github.com/repos/MetaCubeX/Clash.Meta/releases/latest"
        
        # 使用 curl 和 jq 工具解析 API 响应，找到适用于 amd64 Linux 的下载链接
        DOWNLOAD_URL=$(curl -s $LATEST_API_URL | jq -r '.assets[] | select(.name | contains("linux-amd64")) | .browser_download_url')
        
        if [ -z "$DOWNLOAD_URL" ]; then
            echo "错误：无法自动查找最新的 Clash.Meta 下载链接。"
            # 如果自动查找失败，回退到使用一个固定的较新版本
            echo "尝试使用固定的后备链接..."
            DOWNLOAD_URL="https://github.com/MetaCubeX/Clash.Meta/releases/download/v1.18.1/clash.meta-linux-amd64-v1.18.1.gz"
        fi
        
        echo "下载链接: $DOWNLOAD_URL"
        wget -q -O clash.meta.gz "$DOWNLOAD_URL"
        
        echo "解压缩..."
        gunzip clash.meta.gz
        
        # 查找解压后的可执行文件名（它会随着版本变化），并将其重命名为脚本所需的'clash-core'
        EXEC_NAME=$(find . -type f -name "clash.meta-linux-amd64*")
        mv "$EXEC_NAME" flclashyaml/clash-core
        
        echo "赋予执行权限..."
        chmod +x flclashyaml/clash-core
        
        echo "Clash.Meta Core 准备就绪。"

    - name: 运行合并与重命名脚本
      run: |
        python flclashyaml/111.py

    - name: 提交更新到仓库
      run: |
        git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add flclashyaml/choutuan-111.yaml
        # 只有在文件内容发生变化时才提交，避免空提交
        if ! git diff --staged --quiet; then
          git commit -m "🔄 丑团yyy $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          git push
        else
          echo "文件无变化，无需提交。"
        fi
