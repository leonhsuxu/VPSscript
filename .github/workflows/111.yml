name: 🔄 丑团111
on:
  schedule:
    # 每天 UTC 0:00 (北京时间 8:00) 自动运行
    - cron: '0 0 * * *'
  workflow_dispatch: # 允许手动触发
  push:
    branches:
      - main
    paths:
      - 'flclashyaml/111.py'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置 Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: 安装依赖和 tcping 工具
      run: |
        # 1. 安装系统工具 tcping
        sudo apt-get update
        sudo apt-get install -y tcping
        
        # 2. 安装 Python 依赖 (不再需要 PySocks)
        python -m pip install --upgrade pip
        pip install requests pyyaml

    # --- 3. 移除 Xray-core 下载步骤 ---
    # 由于改用 tcping，此步骤已不再需要

    - name: 运行合并与重命名脚本
      run: |
        python flclashyaml/111.py

    - name: 提交更新到仓库
      run: |
        git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add flclashyaml/choutuan-111.yaml
        if ! git diff --staged --quiet; then
          git commit -m "🔄 丑团yyy $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          git push
        else
          echo "文件无变化，无需提交。"
        fi
