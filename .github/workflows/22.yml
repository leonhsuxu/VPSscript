name: Fetch Pastecode Content

on: [push, workflow_dispatch]

jobs:
  fetch-data:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch content from pastecode
        run: |
          curl "https://pastecode.dev/raw/hntbocnp/%E5%81%A5%E5%BA%B7%E4%B8%AD%E5%BF%83618ord" -o data.txt
          cat data.txt
      
      - name: Process the data
        run: |
          # 这里可以添加你的处理脚本
          echo "Data fetched successfully"
