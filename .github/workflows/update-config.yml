name: 更新 Clash 配置

on:
  schedule:
    # 每天 UTC 3:00 (北京时间 11:00) 自动运行
    - cron: '0 3 * * *'
  workflow_dispatch: # 允许手动触发
  push:
    branches:
      - main
    paths:
      - 'merge_subscriptions.py'

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      
    - name: 设置 Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: 安装依赖
      run: |
        pip install requests pyyaml
        
    - name: 运行合并脚本
      run: |
        python merge_subscriptions.py
        
    - name: 提交更新
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add config.yaml
        git diff --quiet && git diff --staged --quiet || (git commit -m "🔄 自动更新配置 $(date +'%Y-%m-%d %H:%M:%S')" && git push)
